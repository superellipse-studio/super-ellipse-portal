// Shared class strings for the sage-card design system, so every page
// that adopts it stays visually consistent without copy-pasting strings.

export const cardClass = "bg-sage text-black rounded-3xl p-5 sm:p-7";

// Small-caps label style for text sitting on the light sage card background —
// separate from the shared `.label` utility, which is tuned for the dark shell.
export const cardLabel = "text-[10px] uppercase tracking-wide text-black/45";
